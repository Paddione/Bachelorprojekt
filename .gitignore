# ── Secrets (niemals committen!) ────────────────
.env
.env.*
!.env.example
!dotfiles/**/.env
# ── Environment secrets — now TRACKED but git-crypt-encrypted (see .gitattributes) ──
# (Was fully ignored; encryption makes it safe to commit into this public repo.)
# prod/secrets.yaml and prod-korczewski/secrets.yaml committed as ArgoCD placeholders (Skip=true)
# Real credentials are applied via: task workspace:prod:deploy / task korczewski:deploy
# k3d/secrets.yaml committed intentionally — dev-only placeholder values, not real credentials
deploy/mcp/openclaw-secrets.yaml
# OpenClaw — runtime config has secrets; only template is committed
openclaw/.env
.openclaw/workspace/
# deploy/mcp/claude-code-secrets.yaml — now TRACKED, git-crypt-encrypted (see .gitattributes)
.mcp.json
.lavish/
vaultwarden-import-*.json
*-bitwarden-export.json
claude-code/*-export.settings.json
k3d/vaultwarden-seed-credentials.yaml
k3d/vaultwarden-seed-job.yaml
# ── Runtime-Daten (Docker-Volumes, DB, Certs) ──
data/
!website/src/data/

# ── Backup-Dumps (Skripte ja, Dumps nein) ──────
backup/*.sql
backup/*.sql.gz
backup/*.tar
backup/*.tar.gz
backup/*.dump

# ── Logs ────────────────────────────────────────
*.log
logs/

# ── OS-Artefakte ────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini

# ── Editor / IDE ────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── WireGuard (generated keys and configs) ──────
wireguard/*.conf
wireguard/*.key
wireguard/*.pub

# ── Docker ──────────────────────────────────────
.docker/
**/target/

# ── Tracking (local SQLite, regenerated from JSON) ──
tracking.db

# ── Website (Astro build output) ───────────────
website/dist/
website/.astro/

# ── Worktrees ───────────────────────────────────
.worktrees/
tmp/

# ── OpenCode / Claude Code local state ──
# Project-side opencode config (committed: opencode.jsonc, local overrides: opencode.local.jsonc)
.opencode/opencode.local.jsonc
# Claude Code local state (settings + hookify rules)
.claude/*.local.md
.claude/settings.local.json
.claude/worktrees/

# ── Misc ────────────────────────────────────────
.git_corrupt/
docs-site/
node_modules
node_modules/
*.tmp
*.bak
docs/superpowers/*
!docs/superpowers/specs/
!docs/superpowers/plans/
!docs/superpowers/references/
files_chore.txt
files_main.txt
website/node_modules.task/
.task/checksum
# prod-korczewski/realm-workspace-korczewski.json tracked — Keycloak realm config (no credentials)
prod/realm-workspace-prod.json
# prod-korczewski/kustomization.yaml tracked — no secrets in this file
prod/realm-workspace-prod.json
website/.superpowers.superpowers/
.superpowers/
.playwright-mcp/
test-results/
tests/e2e/results/outcomes/
.worktrees/
memory
whisper
environments/Assets
# .claude/settings.json is now git-crypt encrypted and tracked
.claude/scheduled_tasks.lock
Systemdesign_Buchhaltung-1.docx
~$stemdesign_Buchhaltung-1.docx
.codex

# Python
__pycache__/
*.pyc
*.pyo

# ── Coaching sources (PDFs/EPUBs, never commit) ──
coaching-sources/*
!coaching-sources/.gitkeep

# ── Coaching scripts (node_modules is symlinked to website/node_modules) ──
scripts/coaching/node_modules

# ── WSL/Windows NTFS alternate data streams ──
*:Zone.Identifier

# docs-content-built HTML is committed; search-index.json is generated (build-docs.mjs:231)
k3d/docs-content-built/search-index.json

# ── BATS vendor library self-tests (not project tests) ──
tests/unit/lib/bats-assert/test/
tests/unit/lib/bats-core/test/
tests/unit/lib/bats-file/test/
tests/unit/lib/bats-support/test/
# docs-to-html plugin output
docs-html-bundles/
.worktrees/

# Gemini CLI agent/skill mirrors — regenerated by `task gemini:sync` from .claude/.
# Tracked symlinks (ccc12ccf) broke worktree stash/rebase ('path beyond a symbolic link').
.gemini/agents/
.gemini/skills/
.gemini
.antigravitycli/57aad7c6-140e-44df-8ff1-e4b0168ac305.json
.github/.openclaw/workspace-state.json
assets/grilling-brett-admin-panel/Brett Design System/_source/placement_spec.json
environments/certs/dev.pem

# ── Raw asset source & scratch (keep local, never commit) ──
# Processed, game-ready assets live in brett/public/assets/ (.glb/.ogg) — those ARE committed.
# These are the bulky raw sources (Blender/FBX/OBJ dumps, sound-library archives) and debug captures.
/assets/new/
/scratch/
/*.zip
/*.7z
dev-iterate-*.png
deeps.sh
deepseek.sh
environments/.secrets/.ssh/known_hosts
.proxmox

# ── Vendored design system committed inside an OpenSpec change (legacy) ──
# mentolder-ds was committed whole (build output + vendor tooling + lockfile) under
# the mentolder-react-rebuild proposal. It must never be re-tracked; if a fresh
# design-system vendor is needed, do it as a proper package or git submodule.
openspec/changes/mentolder-react-rebuild/mentolder-ds/

# Factory injection assets — materialized into the worktree at a phase boundary, never committed.
assets-inbox/docs/superpowers/specs/.gaps/

# Mishap tracker local log (gitignored; use --ticket for persistent records)
.mishaps.log
.worktrees/

# mcp-task-runner build artifacts
mcp-task-runner/bin/

# Design sync converter + output (ds-bundle is build output; .cache/ and node_modules are tooling-local)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
