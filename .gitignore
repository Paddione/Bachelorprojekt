# ── Secrets (niemals committen!) ────────────────
.env
.env.secrets
*.env.local
prod/secrets.yaml
k3d/secrets.yaml
deploy/mcp/openclaw-secrets.yaml
k3d/vaultwarden-seed-credentials.yaml
k3d/vaultwarden-seed-job.yaml
k3d/realm-workspace-dev.json
k3s/realm-workspace-prod.json
prod/realm-workspace-prod.json
prod/wildcard-certificate.yaml
.git_corrupt/

# ── Runtime-Daten (Docker-Volumes, DB, Certs) ──
data/

# ── Backup-Dumps (Skripte ja, Dumps nein) ──────
backup/*.sql
backup/*.sql.gz
backup/*.tar
backup/*.tar.gz
backup/*.dump

# ── Logs ────────────────────────────────────────
*.log
logs/

# ── OS-Artefakte ────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini

# ── Editor / IDE ────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Docker ──────────────────────────────────────
.docker/

# ── Tracking (local SQLite, regenerated from JSON) ──
tracking.db

# ── Misc ────────────────────────────────────────
node_modules/
*.tmp
*.bak
docs/superpowers