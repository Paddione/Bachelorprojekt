# Kore. — preview cards

These are small documentation cards rendered in the Design System tab. Each demonstrates one foundation in isolation: a colour, a type role, a token, a component state cluster.
