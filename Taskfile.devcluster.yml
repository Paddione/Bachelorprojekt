version: "3"

# DECOMMISSIONED — This Taskfile targeted the 3-node devc k3s HA cluster on Proxmox
# (VIP 10.0.0.20, context: devc) which was shelved 2026-05-30 and never built.
# Dev now runs via local k3d (context: k3d-mentolder-dev). See Taskfile.dev-stack.yml.
# Kept as historical reference; all tasks below will fail if run.

vars:
  CTX: k3d-mentolder-dev  # was: devc (dead; devc cluster never built)
  NS: workspace-dev

tasks:
  deploy:
    desc: "[devcluster] DECOMMISSIONED — use 'task dev:cluster:create_legacy' for local k3d instead"
    cmds:
      - |
        echo "ERROR: devcluster tasks target the decommissioned devc k3s HA cluster." >&2
        echo "       Use 'task dev:cluster:create_legacy' for local k3d." >&2
        exit 1

  status:
    desc: "[devcluster] DECOMMISSIONED — use 'task dev:status' for local k3d instead"
    cmds:
      - |
        echo "ERROR: devcluster tasks target the decommissioned devc k3s HA cluster." >&2
        echo "       Use 'task dev:status' for local k3d." >&2
        exit 1
