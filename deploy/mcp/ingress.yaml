# MCP Gateway — single-domain Traefik IngressRoute for all MCP servers
# Access: https://mcp-${PROD_DOMAIN}/{service}/mcp
# Security: IP-restricted to internal networks only
#
# Requires DNS: mcp-${PROD_DOMAIN} → 217.195.149.75

# ── IP Allowlist — restrict MCP access to internal networks ──────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: mcp-ipallow
spec:
  ipAllowList:
    sourceRange:
      - 10.10.0.0/16       # k3s management network (Claude Code host)
      - 10.0.3.0/24         # k3s control-plane nodes
      - 10.0.31.0/24        # k3s worker nodes
      - 172.16.0.0/12       # RFC1918 private (WSL2, Docker, etc.)
---
# ── Strip first path segment: /kubernetes/mcp → /mcp ─────────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: mcp-strip-service-prefix
spec:
  stripPrefixRegex:
    regex:
      - "^/[^/]+"
---
# ── Middleware chain: IP filter + strip + security headers ────────
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: mcp-chain
spec:
  chain:
    middlewares:
      - name: mcp-ipallow
      - name: mcp-strip-service-prefix
      - name: security-headers
        namespace: ${INFRA_NAMESPACE}
      - name: compression
        namespace: ${INFRA_NAMESPACE}
---
# ── IngressRoute: path-based routing to all MCP servers ──────────
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mcp-gateway
spec:
  entryPoints:
    - websecure
  routes:
    # ── Kubernetes MCP ────────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/kubernetes`)
      middlewares:
        - name: mcp-chain
      services:
        - name: openclaw-mcp-core
          port: 8080

    # ── PostgreSQL MCP ────────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/postgres`)
      middlewares:
        - name: mcp-chain
      services:
        - name: openclaw-mcp-core
          port: 3001

    # ── Mattermost MCP ────────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/mattermost`)
      middlewares:
        - name: mcp-chain
      services:
        - name: openclaw-mcp-core
          port: 8000

    # ── Nextcloud MCP ─────────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/nextcloud`)
      middlewares:
        - name: mcp-chain
      services:
        - name: openclaw-mcp-apps
          port: 8000

    # ── Invoice Ninja MCP ─────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/invoiceninja`)
      middlewares:
        - name: mcp-chain
      services:
        - name: openclaw-mcp-apps
          port: 8080

    # ── Keycloak MCP (SSE transport) ──────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/keycloak`)
      middlewares:
        - name: mcp-chain
      services:
        - name: openclaw-mcp-auth
          port: 8080

    # ── GitHub MCP ────────────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/github`)
      middlewares:
        - name: mcp-chain
      services:
        - name: mcp-github
          port: 3002

    # ── Stripe MCP ────────────────────────────────────────────────
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`) && PathPrefix(`/stripe`)
      middlewares:
        - name: mcp-chain
      services:
        - name: mcp-stripe
          port: 3003

    # ── OpenClaw WebUI (catch-all at root) ───────────────────────
    # Traefik routes by specificity: /kubernetes, /postgres etc.
    # match first, everything else falls through to OpenClaw.
    # No stripPrefix needed — OpenClaw serves from /.
    - kind: Rule
      match: Host(`mcp-${PROD_DOMAIN}`)
      priority: 1
      middlewares:
        - name: mcp-ipallow
        - name: security-headers
          namespace: ${INFRA_NAMESPACE}
        - name: compression
          namespace: ${INFRA_NAMESPACE}
      services:
        - name: openclaw
          namespace: workspace
          port: 8080

  tls:
    certResolver: letsencrypt
