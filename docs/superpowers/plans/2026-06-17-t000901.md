---
title: Implementierungsplan — T000901 Autopilot-Qualitäts-Ratchet (DeepSeek-Scout)
ticket_id: T000901
domains: [website, infra, db, ops, test]
status: active
pr_number: null
file_locks: []
shared_changes: false
batch_id: null
parent_feature: null
depends_on_plans: []
---

# Implementierungsplan — T000901 Autopilot-Qualitäts-Ratchet (DeepSeek-Scout)

Spec: `docs/superpowers/specs/2026-06-17-t000901-design.md`
Branch: `feature/t000901`

## Ziel

Zwei additive Bausteine für die Scout-Qualität der Software Factory:
- **A — Hybrid-Scout:** deterministische Discovery in `scout.sh` ausbauen + optionaler DeepSeek-LLM-
  Fallback (eigenes pures Helper-Skript) bei `< N` gefundenen Dateien. Fail-soft, opt-in via Env.
- **B — Drift-Ratchet:** nach Merge `touched_files` (Vorhersage) vs. echter `git diff --name-only`
  (Realität) als Jaccard-Distanz vergleichen, Score in DB loggen, bei Schwellenüberschreitung nur
  Warn-Kommentar. **Kein hartes Gate.**

## Architektur & Tech-Stack

- Bash (`set -uo pipefail`, fail-soft) für `scout.sh`, `scout-llm-fallback.sh`, `scout-drift.sh`.
- Reiner CommonJS-Helper `scout-drift.cjs` (require-bar aus Bats + Skript, keine DB-/Pipeline-Imports
  → **S2**: kein Zyklus).
- Provider-Resolution für DeepSeek ausschließlich über bestehendes `route-provider.sh` (DB-getrieben,
  **keine** Brand-Domain-Literale → **S3**).
- DB: neue Spalten `scout_drift` (NUMERIC) + `scout_drift_at` (TIMESTAMPTZ) auf `tickets.tickets`,
  idempotente Migration. Persistenz/Lesen via `ticket.sh` (analog `set-touched-files`).
- Post-Merge-Hook in bestehendem `.github/workflows/post-merge.yml` (hat bereits Ticket-ID-Extraktion
  + `FLEET_KUBECONFIG` + DB-Zugriff), strikt fail-soft.

### S1-Zeilenbudgets (pro zu ändernder Datei ermittelt)

| Datei | Ist | Wirksame Schwelle | Budget | Maßnahme |
|---|---|---|---|---|
| `scripts/factory/scout.sh` | 197 | 300 (`.sh`-Limit, nicht-baselined) | ~103 | Fallback-Aufruf kompakt (~25 Z.) → ok |
| `scripts/factory/scout-llm-fallback.sh` | **neu** | 300 | Ziel < 150 | mit Wachstumsreserve schneiden |
| `scripts/factory/scout-drift.sh` | **neu** | 300 | Ziel < 180 | reine Orchestrierung; Jaccard ausgelagert |
| `scripts/factory/scout-drift.cjs` | **neu** | 200 (`.cjs`) | Ziel < 120 | purer Helper |
| `scripts/ticket.sh` | 699 | **795 (baseline)** | ~96 | +1 case + fn (~15 Z.) → ok, bleibt < baseline |

> Falls `scout.sh` nach der Änderung > ~240 Zeilen (80 % von 300) landet: LLM-Merge-Logik komplett in
> `scout-llm-fallback.sh` halten (nur Aufruf + Union in `scout.sh`) — kein kosmetisches Zusammenziehen.

## Tasks

### Task 1 — Migration: scout_drift-Spalten (TDD-fern, DDL)
- [ ] `scripts/migrations/2026-06-17-scout-drift.sql` anlegen:
  - [ ] `ALTER TABLE tickets.tickets ADD COLUMN IF NOT EXISTS scout_drift NUMERIC;`
  - [ ] `ALTER TABLE tickets.tickets ADD COLUMN IF NOT EXISTS scout_drift_at TIMESTAMPTZ;`
  - [ ] Kommentar-Header: idempotent, auf **beide** Brand-DBs (`workspace` + `workspace-korczewski`)
    post-merge anzuwenden (analog bestehender Migrationen).
- [ ] Konvention gegen `scripts/migrations/2026-06-15-cockpit-feature-suggest.sql` abgleichen
  (gleiches `ADD COLUMN IF NOT EXISTS`-Muster).

### Task 2 — Pure Drift-Helper (`scout-drift.cjs`) — TDD zuerst
- [ ] **Test zuerst:** `tests/unit/factory-scout-drift.bats` mit `node -e "require(...)"`-Aufrufen
  (oder direkt node-Assertions in Bats) für `jaccardDistance(P, A)` und `filterNoise(paths)`:
  - [ ] `P==A` → `0`
  - [ ] disjunkt (kein Overlap) → `1`
  - [ ] `P` leer, `A` nicht-leer → `1`
  - [ ] beide leer → `0`
  - [ ] Teilüberlappung (z.B. `|∩|=1,|∪|=3`) → `0.666…` (Toleranz)
  - [ ] `filterNoise` entfernt `docs/generated/**`, `docs/code-quality/repo-index.json`,
    `website/src/data/test-inventory.json`, `docs/superpowers/plans/**`, `docs/superpowers/specs/**`
  - [ ] **Test rot sehen** (Helper existiert noch nicht).
- [ ] `scripts/factory/scout-drift.cjs` implementieren:
  - [ ] `jaccardDistance(predicted, actual)` auf Sets, Edge-Cases wie Spec.
  - [ ] `filterNoise(paths)` (pure, kein I/O).
  - [ ] `module.exports = { jaccardDistance, filterNoise }`.
  - [ ] **S2:** keine `require()` auf DB-/Pipeline-Module — rein.
- [ ] Test grün sehen.

### Task 3 — LLM-Fallback-Skript (`scout-llm-fallback.sh`)
- [ ] `scripts/factory/scout-llm-fallback.sh` anlegen (fail-soft, `set -uo pipefail`):
  - [ ] CLI: `--title --slug --description --repo` (gleiche Signatur-Teilmenge wie `scout.sh`).
  - [ ] Provider auflösen via `route-provider.sh factory-scout cheap` (oder vorhandener Scout-Tier);
    liefert kein Provider / `SCOUT_LLM_ENABLED=false` → **Exit 0 mit leerer Pfadliste** (Skip).
  - [ ] LLM-Call mit Timeout (z.B. `timeout 20`), Prompt: „liste wahrscheinlich betroffene Dateien".
  - [ ] Output: eine Datei pro Zeile auf stdout. Halluzination-Filter: nur Pfade ausgeben, für die
    `test -e "$REPO/$path"` wahr ist.
  - [ ] **S3:** keine Brand-Domain-Literale; Base-URL kommt nur aus `route-provider.sh`-JSON.
  - [ ] Jeder Fehlerpfad (Provider weg, Timeout, ungültige JSON) → stderr-Warnung + Exit 0, **leere**
    Ausgabe (deterministischer Scout-Output bleibt unberührt).

### Task 4 — Hybrid-Discovery in `scout.sh`
- [ ] In `scout.sh` nach Phase 2 (File-Discovery), vor Komplexitäts-Klassifikation:
  - [ ] Wenn `${#TOUCHED[@]} < ${SCOUT_LLM_MIN_FILES:-2}` **und** `SCOUT_LLM_ENABLED != false`:
    `scout-llm-fallback.sh` mit denselben Argumenten aufrufen.
  - [ ] Rückgabezeilen → Union mit `TOUCHED` (dedupliziert, weiterhin auf 30 gekappt).
  - [ ] Fallback-Aufruf fail-soft (`|| true`), bei leerer Rückgabe ändert sich nichts.
- [ ] Deterministische Patterns leicht ausbauen (Spec Baustein A): zusätzliche `--include`-Globs für
  `*.mjs`/`*.cjs` in Strategy A; Strategy B Filename-Match case-insensitiv lassen. **Keine** Brand-
  Literale, keine neuen Hardcodes.
- [ ] `wc -l scripts/factory/scout.sh` prüfen: muss < 240 bleiben (sonst mehr in Fallback-Skript ziehen).

### Task 5 — `ticket.sh set-scout-drift` Subcommand
- [ ] In `scripts/ticket.sh` `cmd_set_scout_drift()` analog `cmd_set_touched_files` ergänzen
  (`--id`, `--drift`): `UPDATE tickets.tickets SET scout_drift=:'drift'::numeric, scout_drift_at=now()
  WHERE external_id=:'ext_id';`.
- [ ] Dispatch-`case` + Commands-Usage-Zeile ergänzen (`set-scout-drift`).
- [ ] `wc -l scripts/ticket.sh` prüfen: muss `< 795` (Baseline) bleiben.

### Task 6 — Post-Merge-Orchestrierung (`scout-drift.sh`)
- [ ] `scripts/factory/scout-drift.sh` anlegen:
  - [ ] CLI `--ticket <id> --base <sha> --head <sha> --repo <path>`.
  - [ ] `P` = `ticket.sh get --id <id>` → `touched_files` extrahieren (relativ zu Repo-Root).
  - [ ] `A` = `git -C <repo> diff --name-only <base>..<head>` → durch `scout-drift.cjs filterNoise`.
  - [ ] `drift` via `node -e "require('./scout-drift.cjs')..."` berechnen.
  - [ ] `ticket.sh set-scout-drift --id <id> --drift <d>`.
  - [ ] Wenn `drift > ${SCOUT_DRIFT_THRESHOLD:-0.9}`: `ticket.sh add-comment` (Warn, `visibility internal`),
    **kein** Status-Wechsel, **kein** Exit≠0.
  - [ ] Gesamtes Skript fail-soft: jeder Fehler → Warnung + Exit 0.

### Task 7 — `post-merge.yml` Drift-Step
- [ ] In `.github/workflows/post-merge.yml` nach dem `close-ticket`-Step einen Step ergänzen:
  - [ ] `base`/`head` aus `git rev-parse HEAD~1` / `HEAD` (Merge-Commit) ableiten.
  - [ ] `bash scripts/factory/scout-drift.sh --ticket "$TICKET_ID" --base "$BASE" --head "$HEAD"
    --repo "$PWD" || echo "WARNING: scout-drift failed (non-fatal)."`.
  - [ ] `fetch-depth` ist bereits `2` → reicht für `HEAD~1`.

### Task 8 — Test-Wiring & Doku
- [ ] `Taskfile.yml`: `test:unit:factory-scout-drift` (internal) nach Muster
  `test:unit:factory-scout-quality` anlegen und in das `test:unit`-Aggregat (~Zeile 300) aufnehmen.
- [ ] `tests/local/FA-SF-63-scout-deterministic.bats` erweitern: Test, dass `scout.sh` mit
  `SCOUT_LLM_ENABLED=false` exakt den deterministischen Pfad fährt (kein Crash, gültiges JSON).
- [ ] `scripts/factory/autopilot.env.example`: `SCOUT_LLM_ENABLED`, `SCOUT_LLM_MIN_FILES`,
  `SCOUT_DRIFT_THRESHOLD` mit Defaults + Kommentar dokumentieren.
- [ ] `scripts/factory/README.md`: Abschnitt „Hybrid-Scout + Drift-Ratchet" (Datenfluss, Env-Vars,
  dass Migration auf beide Brand-DBs muss).
- [ ] **S4:** sicherstellen, dass `scout-llm-fallback.sh` (von `scout.sh`), `scout-drift.sh` (von
  `post-merge.yml` + README) und `scout-drift.cjs` (von Bats + `scout-drift.sh`) referenziert sind.

### Task 9 — Finaler Verifikations-Task (Pflicht)
- [ ] `task test:changed` — gezielte Tests für geänderte Domains (vitest/BATS/quality).
- [ ] `./tests/unit/lib/bats-core/bin/bats tests/unit/factory-scout-drift.bats` — neue Drift-Tests.
- [ ] `task test:factory` — FA-SF-63 inkl. Hybrid-Skip-Fall.
- [ ] `task test:inventory` — Test-Inventar regenerieren, `website/src/data/test-inventory.json` committen.
- [ ] `task freshness:regenerate` — generierte Artefakte aktualisieren.
- [ ] `task freshness:check` — CI-Äquivalent inkl. `quality:check` (S1–S4-Ratchet) + Baseline-Assertion.
- [ ] `wc -l` final auf `scout.sh` (< 240) und `ticket.sh` (< 795) bestätigen.
- [ ] Alle grün → Branch ist merge-bereit.

## Verifikation der Akzeptanzkriterien (Mapping)

| AK (Spec) | Task |
|---|---|
| 1, 2, 3 (Hybrid-Scout) | 3, 4, 8 (FA-SF-63 Skip-Test) |
| 4 (Jaccard/Noise) | 2 |
| 5 (Drift-Orchestrierung) | 6 |
| 6 (Migration idempotent) | 1 |
| 7 (post-merge fail-soft) | 7 |
| 8 (CI grün) | 9 |
