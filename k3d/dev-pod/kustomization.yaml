# k3d/dev-pod/kustomization.yaml — Basis des dev-pod-Bundles [T900107]
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: workspace-dev

resources:
  - rbac.yaml
  - pvc.yaml
  - home-pvc.yaml
  - authorized-keys.yaml
  - deployment.yaml
  - service.yaml
