apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nextcloud-notify-push
spec:
  rules:
    - host: files.localhost
      http:
        paths:
          - path: /push
            pathType: Prefix
            backend:
              service:
                name: nextcloud-push
                port:
                  number: 7867
