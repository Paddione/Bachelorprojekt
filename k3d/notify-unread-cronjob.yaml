apiVersion: batch/v1
kind: CronJob
metadata:
  name: notify-unread
  namespace: workspace
  labels:
    app: cronjobs
spec:
  # T016592: alle ausgehenden Benachrichtigungs-E-Mails sind abgeschaltet.
  # Bewusst suspendiert statt entfernt — das Wiedereinschalten soll ein
  # umgelegtes Feld sein, kein Manifest-Neubau. Der Endpunkt
  # /api/cron/notify-unread bleibt bestehen und ist per CRON_SECRET geschuetzt.
  suspend: true
  schedule: "0 */6 * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: notify
              image: curlimages/curl:8.21.0@sha256:7c12af72ceb38b7432ab85e1a265cff6ae58e06f95539d539b654f2cfa64bb13
              securityContext:
                allowPrivilegeEscalation: false
                runAsNonRoot: true
                runAsUser: 65534
                capabilities:
                  drop: ["ALL"]
              command:
                - sh
                - -c
                - |
                  curl -sf -X POST \
                    -H "Authorization: Bearer $${CRON_SECRET}" \
                    -H "Content-Type: application/json" \
                    http://website.${WEBSITE_NAMESPACE}.svc.cluster.local/api/cron/notify-unread
              resources:
                requests:
                  cpu: 10m
                  memory: 32Mi
                limits:
                  memory: 64Mi
              env:
                - name: CRON_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: workspace-secrets
                      key: CRON_SECRET
