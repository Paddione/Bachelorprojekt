apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: videovault-uploads-pvc
  labels:
    app: videovault
spec:
  storageClassName: local-path
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 20Gi
