# Patch existing Mattermost deployment with new features
# Apply with: kubectl patch deployment mattermost -n workspace --patch-file k3s/mattermost-patch.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mattermost
  namespace: workspace
spec:
  template:
    spec:
      containers:
        - name: mattermost
          env:
            # Preserve existing env vars (these are additive)
            # ── Webhooks & Bots ─────────────────────────────
            - name: MM_SERVICESETTINGS_ENABLEINCOMINGWEBHOOKS
              value: "true"
            - name: MM_SERVICESETTINGS_ENABLEOUTGOINGWEBHOOKS
              value: "true"
            - name: MM_SERVICESETTINGS_ENABLEBOTACCOUNTCREATION
              value: "true"
            - name: MM_SERVICESETTINGS_ENABLECUSTOMEMOJI
              value: "true"
            # ── Link Previews ───────────────────────────────
            - name: MM_SERVICESETTINGS_ENABLELINKPREVIEWS
              value: "true"
            - name: MM_SERVICESETTINGS_ENABLEPOSTIMAGEPROXY
              value: "true"
            # ── Guest Access ────────────────────────────────
            - name: MM_GUESTSETTINGS_ENABLE
              value: "true"
            # ── OpenSearch ──────────────────────────────────
            - name: MM_ELASTICSEARCHSETTINGS_ENABLEINDEXING
              value: "true"
            - name: MM_ELASTICSEARCHSETTINGS_ENABLESEARCHING
              value: "true"
            - name: MM_ELASTICSEARCHSETTINGS_ENABLEAUTOCOMPLETE
              value: "true"
            - name: MM_ELASTICSEARCHSETTINGS_CONNECTIONURL
              value: "http://opensearch:9200"
            - name: MM_ELASTICSEARCHSETTINGS_SNIFF
              value: "false"
