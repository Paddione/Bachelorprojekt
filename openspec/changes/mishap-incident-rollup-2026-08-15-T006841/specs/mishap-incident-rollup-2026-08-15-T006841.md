---
title: "mishap-incident-rollup-2026-08-15-T006841 — Mishap-Bundle"
ticket_id: T006841
---

## ADDED Requirements

### Requirement: T006368: OpenSpec-Change spec-junit-shard-ignore nie archiviert — einzige Kopie im Hauptcheckout verschwunden

The rollup bundle SHALL address the mishap "T006368: OpenSpec-Change spec-junit-shard-ignore nie archiviert — einzige Kopie im Hauptcheckout verschwunden" (drift, openspec).

#### Scenario: T006368: OpenSpec-Change spec-junit-shard-ignore nie archiviert — einzige Kopie im Hauptcheckout verschwunden is covered by the bundle

- **GIVEN** a batch entry "T006368: OpenSpec-Change spec-junit-shard-ignore nie archiviert — einzige Kopie im Hauptcheckout verschwunden" (drift, openspec) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: T006348: Plan-Guard-Datei passt nicht zum gemergten Fix — 2/8 Assertions rot

The rollup bundle SHALL address the mishap "T006348: Plan-Guard-Datei passt nicht zum gemergten Fix — 2/8 Assertions rot" (drift, tests/spec/agent-skills).

#### Scenario: T006348: Plan-Guard-Datei passt nicht zum gemergten Fix — 2/8 Assertions rot is covered by the bundle

- **GIVEN** a batch entry "T006348: Plan-Guard-Datei passt nicht zum gemergten Fix — 2/8 Assertions rot" (drift, tests/spec/agent-skills) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: plan-intel s1_limit:0 doppeldeutig (Budget 0 vs. ungated) — Fehlalarm bei .jsonc

The rollup bundle SHALL address the mishap "plan-intel s1_limit:0 doppeldeutig (Budget 0 vs. ungated) — Fehlalarm bei .jsonc" (process, scripts/plan-intel.sh).

#### Scenario: plan-intel s1_limit:0 doppeldeutig (Budget 0 vs. ungated) — Fehlalarm bei .jsonc is covered by the bundle

- **GIVEN** a batch entry "plan-intel s1_limit:0 doppeldeutig (Budget 0 vs. ungated) — Fehlalarm bei .jsonc" (process, scripts/plan-intel.sh) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: eval_harness.ModelBackend ohne CUDA-Transfer — lokaler Adapter-Eval unbrauchbar

The rollup bundle SHALL address the mishap "eval_harness.ModelBackend ohne CUDA-Transfer — lokaler Adapter-Eval unbrauchbar" (degraded, scripts/finetune).

#### Scenario: eval_harness.ModelBackend ohne CUDA-Transfer — lokaler Adapter-Eval unbrauchbar is covered by the bundle

- **GIVEN** a batch entry "eval_harness.ModelBackend ohne CUDA-Transfer — lokaler Adapter-Eval unbrauchbar" (degraded, scripts/finetune) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: fla von PyPI nicht installierbar (Seite 200, API 404)

The rollup bundle SHALL address the mishap "fla von PyPI nicht installierbar (Seite 200, API 404)" (suspicious, scripts/finetune).

#### Scenario: fla von PyPI nicht installierbar (Seite 200, API 404) is covered by the bundle

- **GIVEN** a batch entry "fla von PyPI nicht installierbar (Seite 200, API 404)" (suspicious, scripts/finetune) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: llama-server --hf lädt keine Repos ohne GGUF

The rollup bundle SHALL address the mishap "llama-server --hf lädt keine Repos ohne GGUF" (degraded, llama.cpp).

#### Scenario: llama-server --hf lädt keine Repos ohne GGUF is covered by the bundle

- **GIVEN** a batch entry "llama-server --hf lädt keine Repos ohne GGUF" (degraded, llama.cpp) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: export_gguf.py: unsloth schreibt nach outputs/export_gguf/ — Meldung nennt falschen Pfad

The rollup bundle SHALL address the mishap "export_gguf.py: unsloth schreibt nach outputs/export_gguf/ — Meldung nennt falschen Pfad" (degraded, scripts/finetune/export_gguf.py).

#### Scenario: export_gguf.py: unsloth schreibt nach outputs/export_gguf/ — Meldung nennt falschen Pfad is covered by the bundle

- **GIVEN** a batch entry "export_gguf.py: unsloth schreibt nach outputs/export_gguf/ — Meldung nennt falschen Pfad" (degraded, scripts/finetune/export_gguf.py) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: unsloth-Direkt-Save verwirft MTP-Köpfe — Base-GGUF-Export doppelt gebrochen

The rollup bundle SHALL address the mishap "unsloth-Direkt-Save verwirft MTP-Köpfe — Base-GGUF-Export doppelt gebrochen" (degraded, unsloth).

#### Scenario: unsloth-Direkt-Save verwirft MTP-Köpfe — Base-GGUF-Export doppelt gebrochen is covered by the bundle

- **GIVEN** a batch entry "unsloth-Direkt-Save verwirft MTP-Köpfe — Base-GGUF-Export doppelt gebrochen" (degraded, unsloth) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: GPU-Lock während laufendem Export verloren (Parallel-Session?)

The rollup bundle SHALL address the mishap "GPU-Lock während laufendem Export verloren (Parallel-Session?)" (drift, gpu-lock).

#### Scenario: GPU-Lock während laufendem Export verloren (Parallel-Session?) is covered by the bundle

- **GIVEN** a batch entry "GPU-Lock während laufendem Export verloren (Parallel-Session?)" (drift, gpu-lock) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: post-merge-finalize Schritt 10 meldete Branch bereits entfernt — Restbranch existierte noch

The rollup bundle SHALL address the mishap "post-merge-finalize Schritt 10 meldete Branch bereits entfernt — Restbranch existierte noch" (suspicious, scripts/devflow-post-merge-finalize.sh).

#### Scenario: post-merge-finalize Schritt 10 meldete Branch bereits entfernt — Restbranch existierte noch is covered by the bundle

- **GIVEN** a batch entry "post-merge-finalize Schritt 10 meldete Branch bereits entfernt — Restbranch existierte noch" (suspicious, scripts/devflow-post-merge-finalize.sh) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap
