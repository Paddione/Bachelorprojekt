---
title: "mishap-incident-rollup-2026-08-22-T013328 — Mishap-Bundle"
ticket_id: T013328
---

## ADDED Requirements

### Requirement: Mishap-Rollup-Container-Vermehrung: 11 Collect-Mode-Container parallel (Dedupe-Guard-Verstoß)

The rollup bundle SHALL address the mishap "Mishap-Rollup-Container-Vermehrung: 11 Collect-Mode-Container parallel (Dedupe-Guard-Verstoß)" (drift, mishap-tracker).

#### Scenario: Mishap-Rollup-Container-Vermehrung: 11 Collect-Mode-Container parallel (Dedupe-Guard-Verstoß) is covered by the bundle

- **GIVEN** a batch entry "Mishap-Rollup-Container-Vermehrung: 11 Collect-Mode-Container parallel (Dedupe-Guard-Verstoß)" (drift, mishap-tracker) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: Commit-msg-Hook-Friction: drei Ablehnungen für Plan-only-Commit (Scope-Rätselraten)

The rollup bundle SHALL address the mishap "Commit-msg-Hook-Friction: drei Ablehnungen für Plan-only-Commit (Scope-Rätselraten)" (process, repo/git-hooks).

#### Scenario: Commit-msg-Hook-Friction: drei Ablehnungen für Plan-only-Commit (Scope-Rätselraten) is covered by the bundle

- **GIVEN** a batch entry "Commit-msg-Hook-Friction: drei Ablehnungen für Plan-only-Commit (Scope-Rätselraten)" (process, repo/git-hooks) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: stage-plan-Hilfe verschweigt --no-hold, obwohl eine explizite Hold-Entscheidung Pflicht ist

The rollup bundle SHALL address the mishap "stage-plan-Hilfe verschweigt --no-hold, obwohl eine explizite Hold-Entscheidung Pflicht ist" (degraded, scripts/ticket.sh).

#### Scenario: stage-plan-Hilfe verschweigt --no-hold, obwohl eine explizite Hold-Entscheidung Pflicht ist is covered by the bundle

- **GIVEN** a batch entry "stage-plan-Hilfe verschweigt --no-hold, obwohl eine explizite Hold-Entscheidung Pflicht ist" (degraded, scripts/ticket.sh) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: SDLC-Skripte defaulten noch auf gemma26-throughput nach qwen38-Cutover

The rollup bundle SHALL address the mishap "SDLC-Skripte defaulten noch auf gemma26-throughput nach qwen38-Cutover" (drift, scripts/sdlc).

#### Scenario: SDLC-Skripte defaulten noch auf gemma26-throughput nach qwen38-Cutover is covered by the bundle

- **GIVEN** a batch entry "SDLC-Skripte defaulten noch auf gemma26-throughput nach qwen38-Cutover" (drift, scripts/sdlc) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: llm_proxy_request_log verliert erfolgreiches Dispatch (Blind Spot bei Incident-Analyse)

The rollup bundle SHALL address the mishap "llm_proxy_request_log verliert erfolgreiches Dispatch (Blind Spot bei Incident-Analyse)" (suspicious, llm-proxy/request-log).

#### Scenario: llm_proxy_request_log verliert erfolgreiches Dispatch (Blind Spot bei Incident-Analyse) is covered by the bundle

- **GIVEN** a batch entry "llm_proxy_request_log verliert erfolgreiches Dispatch (Blind Spot bei Incident-Analyse)" (suspicious, llm-proxy/request-log) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: gemma26-throughput-Primary weiterhin als Tab-Agent waehlbar

The rollup bundle SHALL address the mishap "gemma26-throughput-Primary weiterhin als Tab-Agent waehlbar" (drift, opencode-config).

#### Scenario: gemma26-throughput-Primary weiterhin als Tab-Agent waehlbar is covered by the bundle

- **GIVEN** a batch entry "gemma26-throughput-Primary weiterhin als Tab-Agent waehlbar" (drift, opencode-config) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: Carry-over-Eskalations-Tickets haben kryptische Titel aus Task-Zeilen

The rollup bundle SHALL address the mishap "Carry-over-Eskalations-Tickets haben kryptische Titel aus Task-Zeilen" (degraded, factory/mishap-rollup-carryover).

#### Scenario: Carry-over-Eskalations-Tickets haben kryptische Titel aus Task-Zeilen is covered by the bundle

- **GIVEN** a batch entry "Carry-over-Eskalations-Tickets haben kryptische Titel aus Task-Zeilen" (degraded, factory/mishap-rollup-carryover) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: Paralleler Akteur mutiert Hauptcheckout während Hygiene-Lauf — §0-Befund löst sich mid-run auf

The rollup bundle SHALL address the mishap "Paralleler Akteur mutiert Hauptcheckout während Hygiene-Lauf — §0-Befund löst sich mid-run auf" (process, repo/hygiene).

#### Scenario: Paralleler Akteur mutiert Hauptcheckout während Hygiene-Lauf — §0-Befund löst sich mid-run auf is covered by the bundle

- **GIVEN** a batch entry "Paralleler Akteur mutiert Hauptcheckout während Hygiene-Lauf — §0-Befund löst sich mid-run auf" (process, repo/hygiene) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: OpenSpec-Archiv hinterlässt uncommitteten SSOT-Merge — main verliert Requirements aus archiviertem Change (T013528)

The rollup bundle SHALL address the mishap "OpenSpec-Archiv hinterlässt uncommitteten SSOT-Merge — main verliert Requirements aus archiviertem Change (T013528)" (degraded, openspec/archive).

#### Scenario: OpenSpec-Archiv hinterlässt uncommitteten SSOT-Merge — main verliert Requirements aus archiviertem Change (T013528) is covered by the bundle

- **GIVEN** a batch entry "OpenSpec-Archiv hinterlässt uncommitteten SSOT-Merge — main verliert Requirements aus archiviertem Change (T013528)" (degraded, openspec/archive) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap

### Requirement: gh run list findet Checks nicht beim Rollup-Namen — Job-Name ≠ Workflow-Name liefert leere Messung

The rollup bundle SHALL address the mishap "gh run list findet Checks nicht beim Rollup-Namen — Job-Name ≠ Workflow-Name liefert leere Messung" (process, ci/gh-cli).

#### Scenario: gh run list findet Checks nicht beim Rollup-Namen — Job-Name ≠ Workflow-Name liefert leere Messung is covered by the bundle

- **GIVEN** a batch entry "gh run list findet Checks nicht beim Rollup-Namen — Job-Name ≠ Workflow-Name liefert leere Messung" (process, ci/gh-cli) on the rollup container ticket
- **WHEN** the rollup generator produces the cycle change
- **THEN** the bundle SHALL cover the mishap
