apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: factory-workdir
  namespace: workspace-dev
  labels:
    app: factory-runner
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 10Gi
