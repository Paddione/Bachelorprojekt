apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: workspace-dev

resources:
  - ../../k3d/dev-stack
  # Fleet-nativer Factory-Dispatcher [T016433] — ADR-007
  - factory-workdir-pvc.yaml
  - factory-runner.yaml

patches:
  - target:
      kind: Namespace
      name: monitoring
    patch: |-
      $patch: delete
      apiVersion: v1
      kind: Namespace
      metadata:
        name: monitoring
