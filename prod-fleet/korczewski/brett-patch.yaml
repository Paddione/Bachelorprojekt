apiVersion: apps/v1
kind: Deployment
metadata:
  name: brett
spec:
  template:
    spec:
      containers:
        - name: brett
          image: ghcr.io/paddione/workspace-brett@${BRETT_IMAGE_DIGEST}
