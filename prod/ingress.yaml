# ═══════════════════════════════════════════════════════════════════
# Production Ingress — per-Service mit Traefik-Middlewares
# ═══════════════════════════════════════════════════════════════════

# ── Apex Domain → Old Webspace ─────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-apex
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd"
spec:
  tls:
    - hosts:
        - ${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: ${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: old-webspace
                port:
                  number: 443
---
# ── Keycloak (SSO) — Rate-Limiting Brute-Force-Schutz ──────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-auth
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd,workspace-rate-limit-auth@kubernetescrd"
spec:
  tls:
    - hosts:
        - auth.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: auth.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  number: 8080
---
# ── Nextcloud (Dateien) ─────────────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-files
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd,workspace-rate-limit-files@kubernetescrd"
spec:
  tls:
    - hosts:
        - files.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: files.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nextcloud
                port:
                  number: 80
---
# ── Vaultwarden (Passwort-Manager) ─────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-vault
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd,workspace-rate-limit-vault@kubernetescrd"
spec:
  tls:
    - hosts:
        - vault.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: vault.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vaultwarden
                port:
                  number: 80
---
# ── Docs — Keycloak SSO (oauth2-proxy) ─────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-docs
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd"
spec:
  tls:
    - hosts:
        - docs.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: docs.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: oauth2-proxy-docs
                port:
                  number: 4180
---
# ── DocuSeal (E-Signaturen) ─────────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-sign
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd"
spec:
  tls:
    - hosts:
        - sign.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: sign.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: docuseal
                port:
                  number: 3000
---
# ── Restliche Services (meet, board, signaling) ─────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-misc
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd"
spec:
  tls:
    - hosts:
        - meet.${PROD_DOMAIN}
        - board.${PROD_DOMAIN}
        - signaling.${PROD_DOMAIN}
        - bug.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: meet.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spreed-signaling
                port:
                  number: 8080
    - host: board.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: whiteboard
                port:
                  number: 3002
    - host: signaling.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: spreed-signaling
                port:
                  number: 8080
    - host: bug.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  number: 80
---
# ── Tracking (Bachelorprojekt) ──────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-tracking
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd"
spec:
  tls:
    - hosts:
        - tracking.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: tracking.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bachelorprojekt
                port:
                  number: 80
---
# ── Systemisches Brett (3D) ─────────────────────────────────────────
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: workspace-ingress-brett
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: "workspace-redirect-https@kubernetescrd,workspace-hsts-headers@kubernetescrd,workspace-security-headers@kubernetescrd"
spec:
  tls:
    - hosts:
        - brett.${PROD_DOMAIN}
      secretName: ${TLS_SECRET_NAME}
  rules:
    - host: brett.${PROD_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: brett
                port:
                  number: 3000
