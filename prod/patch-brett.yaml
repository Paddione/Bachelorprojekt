apiVersion: apps/v1
kind: Deployment
metadata:
  name: brett
spec:
  template:
    spec:
      containers:
        - name: brett
          env:
            - name: POCKET_ID_PUBLIC_URL
              value: "https://auth.${PROD_DOMAIN}"
