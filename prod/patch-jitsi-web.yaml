apiVersion: apps/v1
kind: Deployment
metadata:
  name: jitsi-web
spec:
  template:
    spec:
      containers:
        - name: web
          env:
            - name: PUBLIC_URL
              value: "https://meet.korczewski.de"
            - name: TOKEN_AUTH_URL
              value: "https://meet.korczewski.de/oidc/auth?state={state}"
