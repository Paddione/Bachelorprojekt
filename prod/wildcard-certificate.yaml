apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: workspace-wildcard
  namespace: workspace
spec:
  secretName: ${TLS_SECRET_NAME}
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.${PROD_DOMAIN}"
    - "${PROD_DOMAIN}"
