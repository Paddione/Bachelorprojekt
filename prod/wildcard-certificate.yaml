apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: workspace-wildcard
  namespace: workspace
spec:
  secretName: workspace-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.wbhprojekt.ipv64.net"
    - "wbhprojekt.ipv64.net"
