#!/usr/bin/env bats
# T001591: opencode-agent-harness (Lavish-Delegation Spawn-Wrapper)
# Status: RED-state scaffolding for T001591 was merged to main — the shell testHarness
# wrapper these cases depend on was never written, so every case failed with "command
# not found" and blocked the required Offline Tests check. Skipped pending real
# implementation; do not delete — re-enable once testHarness exists.

setup() { skip "T001591 not yet implemented — see openspec/changes/t001591"; }
