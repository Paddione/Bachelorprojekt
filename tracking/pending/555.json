{
  "pr_number": 555,
  "title": "consolidate dashboard.${PROD_DOMAIN} into /admin/monitoring",
  "description": "## Summary\nReplaces the standalone Express operator dashboard at `dashboard.${PROD_DOMAIN}` with the existing styled Svelte `/admin/monitoring`. One UI handles everything; the dashboard subdomain is removed.\n\n## What moves into /admin/monitoring\n- **New ArgoCDTab** — sync/health KPIs + per-app table, polling 15s.\n- **New LogsTab** — namespace + pod (+ container) selector + tail viewer.\n- **Bugs tab gains** comment thread, add-comment, and \"Wieder öffnen\" (reopen). The previously broken JSON path through `/api/admin/bugs/{resolve,archive}` is fixed (it was silently 200ing via a 303 redirect chain).\n- **MonitoringDashboard tabs**: Übersicht / Cluster / Deployments / ArgoCD / Logs / Bugs / Tracking. Drops the empty Tasks stub.\n- **admin.astro**: the Dashboard tile (which had replaced the dead Traefik tile in PR #554) now points to `/admin/monitoring` and is renamed Monitoring.\n\n## New API endpoints (all behind getSession + isAdmin)\n- `GET /api/admin/bugs/[id]` — ticket + comments\n- `POST /api/admin/bugs/[id]/comments` — append comment, author = session\n- `POST /api/admin/bugs/reopen` — reopen\n- `POST /api/admin/bugs/resolve` and `archive` — now also accept JSON\n- `GET /api/admin/cluster/argocd-apps` — ArgoCD Applications\n- `GET /api/admin/cluster/logs` — pod logs\n- `GET /api/admin/cluster/pods-list` — compact pod list for the Logs dropdown\n\n## RBAC (`k3d/website.yaml` ClusterRole)\n- + `argoproj.io/applications` [get, list]\n- + core `pods/log` [get]\n\n## Teardown\n- `prod-{mentolder,korczewski}/{dashboard-web,oauth2-proxy-dashboard,ingress-dashboard}.yaml` removed and dropped from each `kustomization.yaml`. ArgoCD `automated.prune+selfHeal` will delete the live resources on next sync.\n- `dashboard/` source dir, `docker/Dockerfile.dashboard-web`, `scripts/dashboard-bootstrap-korczewski.sh`, and the `dashboard:*` / `feature:dashboard` Taskfile entries removed.\n- Left in place (harmless leftovers, removable later): `DASHBOARD_DOMAIN` in `configmap-domains.yaml` / ApplicationSet env / Taskfile envsubst / realm `dashboard` client.\n\n## Test plan\n- [x] Website rebuilt + rolled on both prod clusters; new tabs/strings present in compiled bundle (`ArgoCD`, `Verlauf`, `Wieder öffnen`, `/api/admin/cluster/logs`, `Monitoring`, `/admin/monitoring`).\n- [x] `kubectl auth can-i list applications.argoproj.io --as=…website` → yes (both clusters).\n- [x] All new endpoints return `401` without a session cookie (auth guard correct).\n- [ ] After merge: ArgoCD prunes the 6 dashboard resources from `workspace` and `workspace-korczewski`.\n- [ ] Admin login at https://web.mentolder.de/admin → click \"Monitoring\" → all 7 tabs load; Bugs add-comment/reopen works; ArgoCD lists Applications; Logs shows pod output.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "category": "chore",
  "scope": "monitoring",
  "brand": null,
  "requirement_id": null,
  "merged_at": "2026-05-06T23:27:39Z",
  "merged_by": "Paddione",
  "bug_refs": []
}
