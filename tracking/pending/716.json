{
  "pr_number": 716,
  "title": "single source of truth for art library + popup polish",
  "description": "## Summary\n- Prod brett was rendering 3–4 brand-portfolio characters (mentolder digital50/leadership/consulting; korczewski figure-01..04) and **no category toolbar**, even though PR #714 shipped 35 figures across 6 categories (personen/rollen/abstrakta/symbole/raeume/natur).\n- Root cause: the `art-library` ConfigMap, generated by `k3d/kustomization.yaml` (korczewski portfolio) and `prod-mentolder/kustomization.yaml` (mentolder portfolio, `behavior: replace`), mounted onto `/app/public/art-library` in the brett pod and shadowed the image's baked-in 35-figure manifest.\n- This PR removes the ConfigMap mechanism entirely. The brett image's `brett/public/art-library/` becomes the single source of truth across every cluster.\n- Also folds in an in-flight `#optik-popup` polish: popup becomes `position: fixed`, positioned from the button's bounding rect with viewport-clamping and close-on-resize.\n\n## Bug\nNot a tracked BR-* ticket — `web.mentolder.de/admin/bugs` is in maintenance mode at time of writing.\n\n- Symptom: `brett.mentolder.de` shows 3 chars; `brett.korczewski.de` shows 4. Neither shows the 6-category toolbar. Local/dev shows the full 35-figure set.\n- Expected (per PR #714 intent): 35 figures across 6 category tabs on every cluster.\n- Cause: per-cluster brand-portfolio ConfigMap overlaid the image directory and replaced the merged content with the older schema (`name_de` / `kind`-grouping, no category field).\n\n## Changes\n- `k3d/brett.yaml` — drop `art-library` volumeMount + volumes block.\n- `k3d/kustomization.yaml` — drop `art-library` configMapGenerator (was wiring `art-library/sets/korczewski/portfolio/`).\n- `prod-mentolder/kustomization.yaml` — drop `art-library` configMapGenerator (was wiring `art-library/sets/mentolder/portfolio/` with `behavior: replace`).\n- `brett/public/index.html` — `#optik-popup` viewport-clamp polish.\n\nThe brand-scoped portfolios at `art-library/sets/{mentolder,korczewski}/` stay on disk; they're just no longer wired into brett. Schema validation (`task test:art-library`) still passes.\n\n## Test plan\n- [x] `kubectl kustomize --load-restrictor=LoadRestrictionsNone prod-mentolder` — builds clean (16647 lines, no `name: art-library` ConfigMap, brett Deployment has no `art-library` volume).\n- [x] `kubectl kustomize --load-restrictor=LoadRestrictionsNone prod-korczewski` — builds clean (16257 lines).\n- [x] `kubectl kustomize --load-restrictor=LoadRestrictionsNone k3d` — builds clean (15030 lines).\n- [x] `task workspace:validate ENV=mentolder` — ✓ Manifests are valid.\n- [x] `task workspace:validate ENV=korczewski` — ✓ Manifests are valid.\n- [x] `task test:all` — green.\n- [ ] Post-merge: `task feature:brett` → check `brett.mentolder.de` + `brett.korczewski.de` show the 6 category tabs + 35 figures.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "category": "fix",
  "scope": "brett",
  "brand": null,
  "requirement_id": null,
  "merged_at": "2026-05-13T21:25:11Z",
  "merged_by": "Paddione",
  "bug_refs": []
}
