export function validateSection(_contentKey: string, _payload: unknown): { field: string; message: string }[] {
  return [];
}

export function schemaFor(_contentKey: string): undefined {
  return undefined;
}
