---
// Cockpit cutover: /admin/tickets redirects to cockpit Tabelle mode.
// Auth is handled by cockpit.astro — no redundant session check here.
return Astro.redirect('/admin/cockpit?mode=tabelle');
---
